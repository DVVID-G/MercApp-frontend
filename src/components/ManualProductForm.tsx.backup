import React, { useState, useRef, useEffect } from 'react';
import { motion, AnimatePresence } from 'motion/react';












































































































































































































































































































































































































































































































































































































































































**Aplicable a**: MercApp Frontend (React + Tailwind + Motion)**Versión**: 1.0.0  **Última actualización**: Diciembre 2025  ---**Mantén la coherencia visual sobre todo. Si dudas, observa componentes existentes como `Card.tsx`, `Button.tsx`, `CreatePurchase.tsx` para referenciar patrones establecidos.**6. **Confirma** que cumple el checklist completo5. **Testea** en móvil (o DevTools responsive mode)4. **Prueba** navegación por teclado (Tab, Enter, Escape)3. **Verifica** contraste con herramientas online2. **Reutiliza** clases y patrones de este documento1. **Analiza** el diseño existente en componentes similares**Cada vez que crees un nuevo componente:**## 🎯 Aplicación Práctica---- [WebAIM WCAG 2 Checklist](https://webaim.org/standards/wcag/checklist)- [WCAG 2.1 Quick Reference](https://www.w3.org/WAI/WCAG21/quickref/)### WCAG Guidelines- [AnimatePresence](https://motion.dev/docs/animate-presence)- [Spring Animations](https://motion.dev/docs/animation#spring-animations)- [Motion One Docs](https://motion.dev/)### Framer Motion- [Tailwind Colors](https://tailwindcss.com/docs/customizing-colors)- [Tailwind Spacing Scale](https://tailwindcss.com/docs/customizing-spacing)- [Tailwind Dark Mode](https://tailwindcss.com/docs/dark-mode)### Tailwind CSS- [Coolors Contrast Checker](https://coolors.co/contrast-checker)- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)### Herramientas de Contraste## 📚 Recursos y Referencias```text-gray-500           // Contraste insuficientefont-light on bg-gray-950  // Dificulta legibilidadtext-[15px]             // Usar text-sm (14px) o text-base (16px)// Evitar:```tsx### ❌ Tipografía```gap-[19px]              // Mantener múltiplos de 4pxm-[27px]                // Usar escala estándarp-[13px]                // Usar p-3 (12px) o p-4 (16px)// Evitar valores arbitrarios sin razón:```tsx### ❌ Espaciado```animate-bounce                   // Distrae sin propósitowhileHover={{ scale: 1.5 }}      // Demasiado agresivotransition-all duration-1000     // Demasiado lento// Evitar:```tsx### ❌ Animaciones```border-gray-300 on bg-gray-950   // Contraste débiltext-white on bg-white           // Invisiblebg-gray-50, bg-gray-100          // Solo light themebg-white text-gray-900           // Rompe consistencia// NUNCA en dark theme:```tsx### ❌ Colores## 🚫 Anti-Patrones (NO Hacer)- [ ] Click outside detection para modales/dropdowns- [ ] Disabled states con `opacity-50` y `cursor-not-allowed`- [ ] Toast notifications para feedback inmediato- [ ] Validación con mensajes de error claros- [ ] Estados de loading con indicadores visuales### ✅ Funcionalidad- [ ] Hover/tap effects con `whileHover={{ scale: 1.02 }}`, `whileTap={{ scale: 0.98 }}`- [ ] Motion animations para entradas/salidas (`initial`, `animate`, `exit`)- [ ] Duraciones apropiadas (`duration-150` hover, `duration-200` modales)- [ ] Transiciones suaves con `transition-colors` o `transition-all`### ✅ Animaciones- [ ] Touch-friendly spacing (gap-3 mínimo)- [ ] Max width para contenido (`max-w-[390px]`)- [ ] Padding/spacing responsivo (`p-4 sm:p-6`)- [ ] Breakpoints con `sm:`, `md:`, `lg:` para escalar- [ ] Mobile-first approach (base styles para móvil)### ✅ Responsive- [ ] Estados disabled/loading claramente indicados- [ ] ARIA attributes cuando necesario (`aria-label`, `aria-required`, `role`)- [ ] Labels descriptivos con `htmlFor` y `id` matching- [ ] Focus states visibles para navegación por teclado- [ ] Touch targets mínimo 44x44px (`py-2.5`)- [ ] Contraste mínimo 4.5:1 (texto normal) o 3:1 (texto grande)### ✅ Accesibilidad- [ ] Acentos con `secondary-gold` (#d4af37)- [ ] Focus rings con `focus:ring-2 focus:ring-secondary-gold`- [ ] Hover states con `hover:bg-gray-800` o `hover:border-gray-700`- [ ] Border radius consistente (`rounded-[12px]` para inputs/botones)- [ ] Bordes con `border-2 border-gray-800`- [ ] Usa `bg-gray-950` y `text-white` para tema oscuro### ✅ VisualAntes de considerar completado cualquier componente UI, verificar:## 🎯 Checklist de Implementación```</Button>  Guardando...  <div className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin" /><Button disabled>// Button loading<div className="bg-gray-800 rounded-[8px] h-10 animate-pulse" />// Skeleton<div className="animate-spin rounded-full h-6 w-6 border-2 border-gray-800 border-t-secondary-gold" />// Spinner```tsx### Loading States```<input readOnly className="bg-gray-900 animate-pulse" />// Loading<input disabled className="opacity-50 cursor-not-allowed" />// Disabled<input className="border-2 border-success" />// Success<p className="text-sm text-error mt-1">Campo requerido</p><input className="border-2 border-error focus:ring-error" />// Error<input className="focus:border-secondary-gold focus:ring-2 focus:ring-secondary-gold" />// Focus<input className="border-2 border-gray-800" />// Normal```tsx### Estados de Input## 📋 Validación y Feedback```toast.warning('⚠️ Precio mayor al esperado');// Warningtoast.info('🗑️ Producto eliminado');// Información});  description: errorMessagetoast.error('❌ Error al guardar', {// Error});  description: 'Coca Cola • $15.00'toast.success('✅ Producto agregado', {// Éxitoimport { toast } from 'sonner';```tsx### Usando Sonner## 🔔 Notificaciones (Toast)```</div>  </div>    </span>      ${pum.toFixed(2)}    <span className="text-lg font-bold text-secondary-gold">    </span>      📊 PUM (Precio por {umd})    <span className="text-xs text-gray-400 flex items-center gap-1">  <div className="flex items-center justify-between"><div className="bg-gradient-to-r from-gray-900 to-gray-950 p-3 rounded-[8px] border-2 border-gray-800">```tsx### PUM Display (Precio por Unidad de Medida)```</div>  ))}    </button>      </mark>        {matchedText}      <mark className="bg-secondary-gold bg-opacity-30 text-white">      {/* Highlight matches */}    ">      border-t border-gray-800 first:border-t-0      text-white transition-colors      bg-gray-950 hover:bg-gray-800      w-full px-4 py-3 text-left    <button className="  {suggestions.map((item) => (<div className="absolute z-50 w-full mt-2 bg-gray-950 border-2 border-gray-800 rounded-[12px] shadow-2xl max-h-[280px] overflow-y-auto">// Suggestions dropdown</button>  <X className="w-4 h-4" />">  transition-colors p-1 hover:bg-gray-800 rounded-full  text-gray-400 hover:text-white  absolute right-3 top-1/2 -translate-y-1/2<button className="// Clear button (absoluto dentro del input)" />  placeholder:text-gray-600  focus:ring-2 focus:ring-secondary-gold  rounded-[12px]  bg-gray-950 text-white border-2 border-gray-800  w-full px-4 py-2.5 pr-10<input className="// Input```tsx### Search con Suggestions (Autocomplete)```</div>  ))}    </div>      {/* Item content */}    <div key={item.id} className="py-3 first:pt-0 last:pb-0">  {items.map((item) => (<div className="divide-y divide-gray-800">```tsx### Lista de Items (con separadores)```</div>  </p>    Información destacada  <p className="text-sm font-medium text-white"><div className="bg-gradient-to-r from-yellow-600 to-secondary-gold p-4 rounded-[12px] border-2 border-secondary-gold">```tsx### Highlight de Información Importante```</div>  {/* Contenido */}  </h3>    Precio y Cantidad    <span>💰</span>  <h3 className="text-base font-semibold text-white flex items-center gap-2"><div className="bg-gray-950 border-2 border-gray-800 p-4 rounded-[16px] space-y-3">```tsx### Secciones con Header (Visual Grouping)## 🎨 Patrones Visuales Específicos```space-y-3 sm:space-y-4   // Stack verticalgap-2 sm:gap-3 md:gap-4  // Grid/flex spacing// Gapspx-4 sm:px-6 md:px-8    // Márgenes lateralesp-3 sm:p-4 md:p-6       // Cards, contenedores// Padding```tsx### Espaciado Responsivo```max-w-[1024px]  // Desktop estándarmax-w-[768px]   // Tabletmax-w-[480px]   // Móvil grandemax-w-[390px]   // Móvil (iPhone, Android estándar)```tsx### Max Width para Contenido```className="md:text-sm text-lg"  // Lógica invertida// ❌ Incorrecto: Desktop-first requiere sobrescrituraclassName="grid-cols-1 sm:grid-cols-2 lg:grid-cols-3"className="p-4 md:p-6 lg:p-8"className="text-sm sm:text-base md:text-lg"// ✅ Correcto: Definir móvil primero, luego escalar```tsx### Patrón Mobile-First```xl: 1280px  // Desktopslg: 1024px  // Laptopsmd: 768px   // Tabletssm: 640px   // Teléfonos grandes, landscape```tsx### Breakpoints## 📱 Responsive Design (Mobile-First)```</div>  {errorMessage}<div role="alert" aria-live="polite">// Anuncios para screen readers</button>  {isLoading ? 'Guardando...' : 'Guardar'}<button disabled aria-busy={isLoading}>// Estado de carga</button>  <X className="w-5 h-5" /><button aria-label="Cerrar modal">// Botones descriptivos<input id="product-name" aria-required="true" /></label>  Nombre del Producto *<label htmlFor="product-name" className="block text-sm font-medium text-gray-400 mb-2">// Labels para inputs```tsx### Semantic HTML & ARIA```transition-colors                     // Transición suavefocus:border-secondary-gold           // Borde doradofocus:ring-2 focus:ring-secondary-gold  // Anillo dorado de 2pxfocus:outline-none                    // Remover outline nativo```tsx### Focus States (Navegación por teclado)```gap-3              // 12px mínimo entre elementos táctiles// Separación entre touch targetsmin-w-[44px]       // Botones cuadrados (iconos)min-h-[44px]       // Garantía de altura mínimapy-2.5 px-4        // ≈44px height con padding// Botones y elementos interactivos```tsx### Touch Targets (Mínimo 44x44 pixels)```bg-gray-50, bg-gray-100, bg-gray-200  // Solo para light themetext-gray-300 on bg-gray-950      // Contraste insuficientetext-gray-900 on bg-white         // Solo para light themetext-white on bg-white            // Invisible// NUNCA usar en dark theme:```tsx#### ❌ Combinaciones Prohibidas```text-secondary-gold on bg-gray-950  // #d4af37 on #1a1a1a// Contraste con acentotext-gray-600 on bg-gray-950      // #666 on #1a1a1a// Contraste 4.7:1 (Bueno - WCAG AA)text-gray-400 on bg-gray-950      // #b3b3b3 on #1a1a1a// Contraste 7:1 (Muy Bueno)text-white on bg-gray-950         // #fff on #1a1a1a// Contraste 21:1 (Excelente)```tsx#### ✅ Combinaciones Aprobadas (Dark Theme)### Contraste Mínimo (4.5:1 para texto normal, 3:1 para texto grande)## ♿ Accesibilidad WCAG 2.1 AA```rounded-full    // Avatares, indicadores circularesrounded-[24px]  // Bottom sheets, contenedores grandesrounded-[20px]  // Modales, overlaysrounded-[16px]  // Cards, contenedores medianosrounded-[12px]  // Botones estándar, inputs, dropdownsrounded-[8px]   // Botones pequeños, iconos interactivosrounded-[6px]   // Tags, pillsrounded-[4px]   // Badges pequeños, separadores```tsx## 🔲 Border Radius (Consistencia Visual)```ease-in-out           // Suavizado naturalduration-300          // Lento (transiciones complejas)duration-200          // Estándar (modales, dropdowns)duration-150          // Rápido (hover, feedback inmediato)transition-transform   // Movimiento, escalatransition-all         // Cambios múltiples (hover complejo)transition-colors      // Cambios de color (hover, focus)```tsx### Clases de Transición CSS```>  transition={{ delay: index * 0.05 }}  animate={{ opacity: 1, x: 0 }}  initial={{ opacity: 0, x: -20 }}<motion.div// Lista items (stagger animation)whileTap={{ scale: 0.98 }}whileHover={{ scale: 1.02 }}// Hover states (botones, cards)transition={{ type: 'spring', damping: 25, stiffness: 300 }}exit={{ opacity: 0, scale: 0.9 }}animate={{ opacity: 1, scale: 1 }}initial={{ opacity: 0, scale: 0.9 }}// Spring physics (modales importantes, overlays)transition={{ duration: 0.2, ease: 'easeOut' }}exit={{ opacity: 0, y: -10, scale: 0.95 }}animate={{ opacity: 1, y: 0, scale: 1 }}initial={{ opacity: 0, y: -10, scale: 0.95 }}// Entrada/salida estándar (modales, dropdowns)```tsx### Configuración de Motion (Framer Motion)## 🎬 Animaciones y Transiciones```</motion.div>  {/* Handle indicator */}  <div className="w-12 h-1 bg-gray-800 rounded-full mx-auto mb-6" />">  max-h-[90vh] overflow-y-auto  rounded-t-[24px] p-6 z-50  bg-gray-950 border-t-2 border-gray-800  fixed bottom-0 left-0 right-0 max-w-[390px] mx-auto<motion.div className="// Bottom Sheet (móvil)">  max-h-[90vh] overflow-y-auto  rounded-[20px] p-6 z-50  bg-gray-950 border-2 border-gray-800  w-[calc(100%-48px)] max-w-[390px]  fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2<motion.div className="// Modal centrado<div className="fixed inset-0 bg-black bg-opacity-60 backdrop-blur-sm z-40" />// Overlay de fondo```tsx### Modal / Sheet (Overlays)```</span>  Badge">  rounded-[6px] text-xs  bg-gray-800 text-gray-400  px-2 py-1<span className="```tsx### Badge (Etiquetas, Tags)```">  text-white  border-l-4 border-secondary-gold  bg-secondary-gold bg-opacity-20<button className="// Opción seleccionada">  hover:pl-5  // Animación sutil al hover  border-t border-gray-800  // Separadores entre opciones  bg-gray-950 hover:bg-gray-800  transition-all duration-150  w-full px-4 py-3 text-left font-medium text-white<button className="// Opciones">  max-h-60 overflow-hidden  rounded-[12px] shadow-2xl  bg-gray-950 border-2 border-gray-800  absolute z-50 w-full mt-2<motion.div className="// Dropdown container">  text-left flex items-center justify-between  transition-colors  hover:border-gray-700  focus:ring-2 focus:ring-secondary-gold focus:border-secondary-gold  rounded-[12px]  border-2 border-gray-800  bg-gray-950 text-white  w-full px-4 py-2.5<button className="// Botón trigger```tsx### Select / Dropdown (CustomSelect)```" />  transition-colors  disabled:opacity-50 disabled:cursor-not-allowed  placeholder:text-gray-600  focus:outline-none focus:ring-2 focus:ring-secondary-gold focus:border-secondary-gold  rounded-[12px]  border-2 border-gray-800  bg-gray-950 text-white  w-full px-4 py-2.5<input className="```tsx### Input (Campos de Formulario)```</Button>  // bg-transparent text-gray-400 hover:text-white<Button variant="ghost">// Ghost: Acciones terciarias, navegación</Button>  // hover:border-secondary-gold  // bg-gray-950 border-2 border-gray-800 text-white<Button variant="secondary">// Secondary: Acciones secundarias</Button>  // hover:bg-secondary-gold hover:text-primary-black  // bg-primary-black border-2 border-secondary-gold text-white<Button variant="primary">// Primary: Acción principal, CTAs```tsx### Button (Botones)```<Card onClick={handler} className="hover:border-gray-700 cursor-pointer">// Variante con hover (clickeable)</Card>  {children}<Card className="bg-gray-950 border-2 border-gray-800 rounded-[16px] p-4">```tsx### Card (Contenedor Principal)## 🎯 Componentes Base```text-secondary-gold  // Acentos, links, elementos interactivos (#d4af37)text-gray-600    // Texto terciario, placeholders (#666)text-gray-400    // Texto secundario (#b3b3b3) - Contraste 7:1 sobre gray-950text-white       // Texto principal (#fff) - Contraste 21:1 sobre gray-950```tsx### Colores de Texto (Dark Theme)```leading-relaxed  (1.625)  // Párrafos largos, contenido extensoleading-normal   (1.5)    // Texto regular (óptimo para legibilidad)leading-tight    (1.25)   // Títulos, subtítulosleading-none     (1)      // Títulos grandes, diseño compacto```tsx### Line Heights```font-bold     (700)  // Títulos principales, CTAsfont-semibold (600)  // Títulos de secciones, destacadosfont-medium   (500)  // Labels, botones, énfasis moderadofont-normal   (400)  // Texto regular, descripciones```tsx### Pesos de Fuente```text-3xl     (30px)  // Pantallas herotext-2xl     (24px)  // Títulos principalestext-xl      (20px)  // Títulos de seccionestext-lg      (18px)  // Subtítulos, destacadostext-base    (16px)  // Texto principal, formulariostext-sm      (14px)  // Descripciones, texto secundariotext-xs      (12px)  // Badges, metadatos, labels pequeños```tsx### Jerarquía de Tamaños## 🔤 Tipografía```py-2.5               // Botones y elementos interactivos (min 44px height)px-3 sm:px-4         // Inputs y campos de formulariop-4 sm:p-6           // Cards y contenedores// Móvil primero, luego escalar```tsx### Padding Responsivo```--spacing-10: 40px;   /* Separación de componentes mayores */--spacing-8: 32px;    /* Espaciado de páginas */--spacing-6: 24px;    /* Márgenes grandes */--spacing-5: 20px;    /* Separación entre secciones */--spacing-4: 16px;    /* Padding de cards, botones */--spacing-3: 12px;    /* Padding estándar */--spacing-2: 8px;     /* Padding interno pequeño */--spacing-1: 4px;     /* Gaps mínimos */--spacing-0.5: 2px;   /* Separadores finos */```css### Escala de Spacing (basada en 4px)## 📐 Sistema de Espaciado```from-gray-900 to-gray-950          /* Fondos sutiles, secciones */from-yellow-600 to-secondary-gold  /* Botones premium, CTAs principales */```css### Gradientes de Acento (Opcional, para elementos especiales)```--color-black: #000;       /* Solo para casos excepcionales */--color-white: #fff;       /* Texto principal sobre fondos oscuros */--color-warning: #f1c40f;  /* Advertencias, información importante */--color-error: #e74c3c;    /* Errores, alertas */--color-success: #2ecc71;  /* Confirmaciones, éxito */```css### Colores de Estado```--color-gray-50: #fafafa;   /* (NUNCA usar en dark theme) */--color-gray-100: #f5f5f5;  /* (NUNCA usar en dark theme) */--color-gray-200: #e6e6e6;  /* (NUNCA usar en dark theme) */--color-gray-300: #ccc;      /* Bordes sutiles (NUNCA usar en dark theme) */--color-gray-400: #b3b3b3;  /* Iconos terciarios */--color-gray-500: #808080;  /* Elementos deshabilitados */--color-gray-600: #666;      /* Iconos secundarios, placeholders */--color-gray-700: #4d4d4d;  /* Texto secundario, descripciones */--color-gray-800: #333;      /* Bordes, separadores, fondos de badges */--color-gray-900: #1f1f1f;  /* Fondos secundarios, hover states */--color-gray-950: #1a1a1a;  /* Fondos de cards, modales, contenedores principales */```css### Escala de Grises (Background & Surfaces)```--color-secondary-gold: #d4af37;   /* Acentos, highlights, elementos interactivos */--color-primary-black: #0d0d0d;    /* Fondo principal, botones primarios */```css### Colores Primarios (Dark Theme)## 🎨 Paleta de ColoresEsta guía define el sistema de diseño completo de MercApp para garantizar consistencia visual, accesibilidad y una experiencia de usuario profesional en todas las funcionalidades.# MercApp - Sistema de Diseño y Guía de UI/UX---
export interface ManualProductFormData {
  name: string;
  marca: string;
  price: number;
  packageSize: number;
  umd: string;
  barcode: string;
  categoria: string;
}

export interface ManualProductFormProps {
  onSubmit: (data: ManualProductFormData) => void;
  onCancel: () => void;
  isLoading?: boolean;
  initialData?: Partial<ManualProductFormData>;
}

const UMD_OPTIONS = [
  { value: 'kg', label: 'Kilogramos (kg)' },
  { value: 'gramos', label: 'Gramos (g)' },
  { value: 'litros', label: 'Litros (L)' },
  { value: 'ml', label: 'Mililitros (ml)' },
  { value: 'unidad', label: 'Unidad' },
];

const CATEGORIA_OPTIONS = [
  { value: 'Fruver', label: '🥬 Fruver' },
  { value: 'Lácteos', label: '🥛 Lácteos' },
  { value: 'Granos', label: '🌾 Granos' },
  { value: 'Carnes', label: '🥩 Carnes' },
  { value: 'Panadería', label: '🥖 Panadería' },
  { value: 'Bebidas', label: '🥤 Bebidas' },
  { value: 'Aseo', label: '🧼 Aseo' },
  { value: 'Higiene', label: '🧴 Higiene' },
  { value: 'Snacks', label: '🍿 Snacks' },
  { value: 'Condimentos', label: '🧂 Condimentos' },
  { value: 'Otros', label: '📦 Otros' },
];

// Custom Select Component
interface CustomSelectProps {
  value: string;
  onChange: (value: string) => void;
  options: { value: string; label: string }[];
  disabled?: boolean;
  placeholder?: string;
}

function CustomSelect({ value, onChange, options, disabled = false, placeholder }: CustomSelectProps) {
  const [isOpen, setIsOpen] = useState(false);
  const containerRef = useRef<HTMLDivElement>(null);

  const selectedOption = options.find(opt => opt.value === value);

  useEffect(() => {
    const handleClickOutside = (event: MouseEvent) => {
      if (containerRef.current && !containerRef.current.contains(event.target as Node)) {
        setIsOpen(false);
      }
    };

    if (isOpen) {
      document.addEventListener('mousedown', handleClickOutside);
      return () => document.removeEventListener('mousedown', handleClickOutside);
    }
  }, [isOpen]);

  const handleSelect = (optionValue: string) => {
    onChange(optionValue);
    setIsOpen(false);
  };

  return (
    <div ref={containerRef} className="relative">
      <button
        type="button"
        onClick={() => !disabled && setIsOpen(!isOpen)}
        disabled={disabled}
        className="w-full px-4 py-2.5 bg-gray-950 text-white border-2 border-gray-800 rounded-[12px] focus:outline-none focus:ring-2 focus:ring-secondary-gold focus:border-secondary-gold hover:border-gray-700 transition-colors disabled:opacity-50 disabled:cursor-not-allowed text-left flex items-center justify-between"
      >
        <span className="block truncate">
          {selectedOption?.label || placeholder || 'Seleccionar...'}
        </span>
        <svg
          className={`w-5 h-5 text-gray-400 transition-transform ${isOpen ? 'rotate-180' : ''}`}
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
        </svg>
      </button>

      <AnimatePresence>
        {isOpen && (
          <motion.div
            initial={{ opacity: 0, y: -10, scale: 0.95 }}
            animate={{ opacity: 1, y: 0, scale: 1 }}
            exit={{ opacity: 0, y: -10, scale: 0.95 }}
            transition={{ duration: 0.2, ease: 'easeOut' }}
            className="absolute z-50 w-full mt-2 bg-gray-950 border-2 border-gray-800 rounded-[12px] shadow-2xl max-h-60 overflow-hidden"
          >
            <div className="overflow-y-auto max-h-60 scrollbar-thin">
              {options.map((option, index) => (
                <button
                  key={option.value}
                  type="button"
                  onClick={() => handleSelect(option.value)}
                  className={`
                    w-full px-4 py-3 text-left font-medium text-white
                    transition-all duration-150 ease-in-out
                    ${option.value === value 
                      ? 'bg-secondary-gold bg-opacity-20 border-l-4 border-secondary-gold' 
                      : 'bg-gray-950 hover:bg-gray-800'
                    }
                    ${index !== 0 ? 'border-t border-gray-800' : ''}
                    hover:pl-5
                  `}
                >
                  <span className="block text-base">{option.label}</span>
                </button>
              ))}
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
}

export function ManualProductForm({
  onSubmit,
  onCancel,
  isLoading = false,
  initialData = {},
}: ManualProductFormProps) {
  const [formData, setFormData] = useState<ManualProductFormData>({
    name: initialData.name || '',
    marca: initialData.marca || '',
    price: initialData.price || 0,
    packageSize: initialData.packageSize || 0,
    umd: initialData.umd || 'unidad',
    barcode: initialData.barcode || '',
    categoria: initialData.categoria || 'Otros',
  });

  const [errors, setErrors] = useState<Partial<Record<keyof ManualProductFormData, string>>>({});

  const validateForm = (): boolean => {
    const newErrors: Partial<Record<keyof ManualProductFormData, string>> = {};

    if (!formData.name.trim()) {
      newErrors.name = 'El nombre es obligatorio';
    }

    if (!formData.marca.trim()) {
      newErrors.marca = 'La marca es obligatoria';
    }

    if (formData.price <= 0) {
      newErrors.price = 'El precio debe ser mayor a 0';
    }

    if (formData.packageSize <= 0) {
      newErrors.packageSize = 'El tamaño del paquete debe ser mayor a 0';
    }

    if (!formData.barcode.trim()) {
      newErrors.barcode = 'El código de barras es obligatorio';
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (validateForm()) {
      onSubmit(formData);
    }
  };

  const handleChange = (field: keyof ManualProductFormData, value: string | number) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => ({ ...prev, [field]: undefined }));
    }
  };

  const calculatedPUM = formData.price > 0 && formData.packageSize > 0
    ? (formData.price / formData.packageSize).toFixed(2)
    : '0.00';

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: 20 }}
      transition={{ duration: 0.3 }}
      className="bg-white rounded-2xl shadow-lg p-6"
    >
      <div className="mb-6">
        <h2 className="text-2xl font-bold text-gray-900 mb-2 flex items-center gap-2">
          <span className="text-3xl">➕</span>
          <span>Crear Producto Manualmente</span>
        </h2>
        <p className="text-sm text-gray-600">
          Completa todos los campos para agregar un nuevo producto al catálogo
        </p>
      </div>

      <form onSubmit={handleSubmit} className="space-y-5">
        {/* Name */}
        <div>
          <label htmlFor="name" className="block text-sm font-medium text-gray-700 mb-1">
            Nombre del Producto *
          </label>
          <input
            id="name"
            type="text"
            value={formData.name}
            onChange={(e) => handleChange('name', e.target.value)}
            className={`w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary-gold ${
              errors.name ? 'border-red-500' : 'border-gray-300'
            }`}
            placeholder="Ej: Leche Entera"
            disabled={isLoading}
          />
          {errors.name && <p className="mt-1 text-sm text-red-600">{errors.name}</p>}
        </div>

        {/* Marca */}
        <div>
          <label htmlFor="marca" className="block text-sm font-medium text-gray-700 mb-1">
            Marca *
          </label>
          <input
            id="marca"
            type="text"
            value={formData.marca}
            onChange={(e) => handleChange('marca', e.target.value)}
            className={`w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary-gold ${
              errors.marca ? 'border-red-500' : 'border-gray-300'
            }`}
            placeholder="Ej: La Serenísima"
            disabled={isLoading}
          />
          {errors.marca && <p className="mt-1 text-sm text-red-600">{errors.marca}</p>}
        </div>

        {/* Price and Package Size Row */}
        <div className="bg-gray-50 p-4 rounded-lg border border-gray-200">
          <h3 className="text-sm font-semibold text-gray-700 mb-3">💰 Precio y Cantidad</h3>
          <div className="grid grid-cols-2 gap-4">
          <div>
            <label htmlFor="price" className="block text-sm font-medium text-gray-700 mb-1">
              Precio ($) *
            </label>
            <input
              id="price"
              type="number"
              step="0.01"
              min="0"
              value={formData.price || ''}
              onChange={(e) => handleChange('price', parseFloat(e.target.value) || 0)}
              className={`w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary-gold ${
                errors.price ? 'border-red-500' : 'border-gray-300'
              }`}
              placeholder="0.00"
              disabled={isLoading}
            />
            {errors.price && <p className="mt-1 text-sm text-red-600">{errors.price}</p>}
          </div>

          <div>
            <label htmlFor="packageSize" className="block text-sm font-medium text-gray-700 mb-1">
              Tamaño *
            </label>
            <input
              id="packageSize"
              type="number"
              step="0.01"
              min="0"
              value={formData.packageSize || ''}
              onChange={(e) => handleChange('packageSize', parseFloat(e.target.value) || 0)}
              className={`w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary-gold ${
                errors.packageSize ? 'border-red-500' : 'border-gray-300'
              }`}
              placeholder="0"
              disabled={isLoading}
            />
            {errors.packageSize && <p className="mt-1 text-sm text-red-600">{errors.packageSize}</p>}
          </div>
          </div>
        </div>

        {/* Classification Section */}
        <div className="bg-gray-50 p-4 rounded-lg border border-gray-200 space-y-4">
          <h3 className="text-sm font-semibold text-gray-700 mb-3">🏷️ Clasificación</h3>
          
          {/* UMD */}
          <div>
            <label className="block text-sm font-medium text-gray-700 mb-1">
              Unidad de Medida *
            </label>
            <CustomSelect
              value={formData.umd}
              onChange={(value) => handleChange('umd', value)}
              options={UMD_OPTIONS}
              disabled={isLoading}
              placeholder="Selecciona una unidad"
            />
          </div>

        {/* PUM Display */}
        <div className="bg-gradient-to-r from-blue-50 to-indigo-50 border-2 border-blue-200 rounded-lg p-4 shadow-sm">
          <div className="flex items-center justify-between">
            <div>
              <span className="text-xs font-semibold text-blue-600 uppercase tracking-wide block mb-1">
                📊 Cálculo Automático
              </span>
              <span className="text-sm font-medium text-blue-700">PUM (Precio por Unidad)</span>
            </div>
            <span className="text-2xl font-bold text-blue-900 tabular-nums">
              ${calculatedPUM}<span className="text-base text-blue-700">/{formData.umd}</span>
            </span>
          </div>
        </div>

        {/* Barcode */}
        <div>
          <label htmlFor="barcode" className="block text-sm font-medium text-gray-700 mb-1 flex items-center gap-1">
            <span>🔖</span>
            <span>Código de Barras *</span>
          </label>
          <input
            id="barcode"
            type="text"
            value={formData.barcode}
            onChange={(e) => handleChange('barcode', e.target.value)}
            className={`w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-secondary-gold ${
              errors.barcode ? 'border-red-500' : 'border-gray-300'
            }`}
            placeholder="Ej: 7790001234567"
            disabled={isLoading}
          />
          {errors.barcode && <p className="mt-1 text-sm text-red-600">{errors.barcode}</p>}
        </div>

        {/* Categoria */}
        <div>
          <label className="block text-sm font-medium text-gray-700 mb-1">
            Categoría *
          </label>
          <CustomSelect
            value={formData.categoria}
            onChange={(value) => handleChange('categoria', value)}
            options={CATEGORIA_OPTIONS}
            disabled={isLoading}
            placeholder="Selecciona una categoría"
          />
        </div>
        </div>

        {/* Action Buttons */}
        <div className="flex gap-3 pt-4">
          <button
            type="button"
            onClick={onCancel}
            className="flex-1 px-4 py-3 bg-gray-200 hover:bg-gray-300 text-gray-700 rounded-lg font-medium transition-colors"
            disabled={isLoading}
          >
            Cancelar
          </button>
          <button
            type="submit"
            className="flex-1 px-4 py-3 bg-secondary-gold hover:bg-yellow-600 text-white rounded-lg font-medium transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
            disabled={isLoading}
          >
            {isLoading ? (
              <span className="flex items-center justify-center">
                <div className="w-5 h-5 border-2 border-white border-t-transparent rounded-full animate-spin mr-2" />
                Guardando...
              </span>
            ) : (
              'Crear Producto'
            )}
          </button>
        </div>
      </form>
    </motion.div>
  );
}

export default ManualProductForm;
